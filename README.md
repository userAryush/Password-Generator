# Python Password Generator

A simple Python script to generate strong, customizable passwords based on user preferences.

## Features

- Specify **minimum password length**
- Optionally include:
  - Numbers (`0-9`)
  - Special characters (`!@#$%^&*`, etc.)
- Random and secure password generation using Python’s `random` and `string` modules
